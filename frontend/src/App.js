import React, { useState} from 'react';

import './global.css'
import Routes from './routes'

function App() {
  // Retorna um Array com duas posições [valor, função de atualização do valor]
  //const [counter, setCounter] = useState(0);
  //function increment (){
  //  setCounter(counter + 1);
    //console.log(counter)
  //}
  return (  //Entendendo STATE
    <Routes/>
  );
}

export default App;
